package ${package_name}.service;

import com.yj.jenkinsdemo.base.BaseServiceMock;
import ${package_name}.mapper.${model}Mapper;
import ${package_name}.repository.${model}Repository;
import ${package_name}.entity.${model};
import ${package_name}.entity.${model}Example;

/**
* 降级实现${model}Service接口
* Created by yangjia on ${ctime}.
*/
public class ${model}ServiceMock extends BaseServiceMock<${model}Repository,${model}Mapper, ${model}, ${model}Example,String> implements ${model}Service {

}
